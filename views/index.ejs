<!DOCTYPE html>
<html>
  <head>
    <title>Authing Demo</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div>
      当前状态：<%= isLogin ? '已登录' : '未登录' %>
      <% if (!isLogin) { %>
        <a href="/login">登录</a>
      <% } else { %>
        <a href="/logout">登出</a>
        <div>
          用户信息：<a href="/profile">查看</a>
        </div>
      <% } %>
    </div>
  </body>
</html>
